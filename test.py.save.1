file = raw_input("Please enter a file name")
a = []
for line in open(file).readlines()
	a.extend(line.split())
b = 0
for word in a
	if(word.endswith("!")This is a single discussion item for the two weeks and you are only expected to submit an initial response during Week 9  and at least two feedback responses by the end of Week 10 (Spring Break week).
